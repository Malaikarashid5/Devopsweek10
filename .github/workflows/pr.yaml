name: pr-validation
on: 
 pull_request:
       types:
         - opened
         - synchronize

 push:
   branches:
       - develop
jobs:
 pr-validation-job:
    runs-on: ubuntu-latest
    steps:
      - name: welcome steps
        run: echo "welcome to github actions"
      - name: Second step
        run: echo "This is second step"